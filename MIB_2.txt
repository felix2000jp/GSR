    // We will only use tables here
    // Perhaps we could give it a header avoid 
    // searching for commands we don't have

TABLE: generic_table_name {
    SYNTAX: syntax;
    MAX-ACCESS: access;
    STATUS: current;
    DESCRIPTION: description;
}

ENTRY: generic_entry_name {
    SYNTAX: syntax;
    MAX-ACCESS: access;
    STATUS: current;
    DESCRIPTION: description;
    INDEX: starting_column; 
    SEQUENCE: [
        column_name: type,
        ...
    ]
} = { generic_table_name_number }

PARAM: generic_param_name {
    SYNTAX: syntax;
    MAX-ACCESS: access;
    STATUS: current;
    DESCRIPTION: description;
} = { line_index_number}